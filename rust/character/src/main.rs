fn main() {
    // Rust's char type is four bytes in size
    let c = 'z';  // literals with single quotes
    let z = 'Z';
    let heart_eyed_cat = '😻';
    println!("{}", heart_eyed_cat);  // char represents a Unicode Scalar Value
}
